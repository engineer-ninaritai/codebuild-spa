version: 0.2

phases:
  install:
    commands:
      - npm install
      - cd app
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - - aws s3 rm s3://www.engineer-ninaritai.com --recursive
artifacts:
  files:
    - '**/*'
  base-directory: 'dist'
